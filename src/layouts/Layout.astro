<!doctype html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width" />

  <!-- OGP基本情報 -->
  <meta property="og:title" content="MokuLog" />
  <meta property="og:type" content="website" />
  <meta property="og:image" content={new URL("/OGPcard01.PNG", Astro.site).toString()} />
  <meta property="og:url" content={new URL(Astro.url.pathname, Astro.site).toString()}/>
  <meta property="og:image:width" content="1200" />
  <meta property="og:image:height" content="630" />
  <meta property="og:image:type" content="image/png" />
  <meta property="og:locale" content="ja_JP" />
  <meta property="og:description" content="quiet mornings. steady focus." />

  <!-- Twitterカード -->
  <meta name="twitter:card" content="summary_large_image" />

  <!-- Favicon -->
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

  <!-- フォントとスタイル -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+JP:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/styles/globals.css">

  <!-- Astro generator（必要なら値を手動で入れる） -->
  <meta name="generator" content="Astro v4.0" />

  <title>MokuLog</title>
</head>
<body>
  <!-- Astroなどのコンポーネントでここに内容が挿入される -->
  <slot />
</body>
</html>

<style>
  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
  }
</style>